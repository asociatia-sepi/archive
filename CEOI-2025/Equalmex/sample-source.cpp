#include <vector>

std::vector<int> solve(int n, std::vector<int>& v, int q, std::vector<std::pair<int,int>>& queries)
{
	std::vector<int> res;
	for(int i=0;i<q;i++)
		res.push_back(i);
	return res;
}
