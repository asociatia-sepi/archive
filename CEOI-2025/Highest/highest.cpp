#include <vector>

std::vector<int> solve(
        std::vector<int>& v,
        std::vector<int>& w,
        std::vector<std::pair<int, int>>& queries
) {
    int n = v.size();
    int m = queries.size();

    // Implementation goes here.

    return {2, 3, 4};
}


